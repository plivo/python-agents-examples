"""
Live call tests for Gemini 3.1 Flash Live voice agent.

These tests place real calls through Plivo and verify the agent works end-to-end.

Requirements:
    - Valid GEMINI_API_KEY, PLIVO_AUTH_ID, PLIVO_AUTH_TOKEN, PLIVO_PHONE_NUMBER
    - PLIVO_TEST_NUMBER (second number to receive/place calls)
    - ngrok installed and available

Usage:
    cd gemini3.1flash-live-native
    uv run pytest tests/test_live_call.py -v -s
"""

from __future__ import annotations

# TODO: Implement live call tests — copy pattern from grok-voice-native/tests/test_live_call.py
